# یادداشتِ زمینه‌ی پروژه (برای شروعِ سریع در یک نشست/ابزار جدید)

این فایل خلاصه‌ی همه‌ی تصمیم‌ها و الگوریتم‌هایی است که تا الان روی این پروژه گرفته
شده. اگر با یک نشست تازه (مثلاً Claude Code) کار را ادامه می‌دهی، همین یک فایل را
بخوان تا کامل در جریان باشی.

## هدف کلی

اپ وب «جفر جامع» — یک Cloudflare Worker که:
1. کتابِ جفر جامع را مرور می‌کند (بدون ذخیره‌سازی؛ کاملاً محاسباتی).
2. ابزارِ «استخراج مستحصله» دارد که از روی یک سؤال/اسم، به یک خانه از کتاب یا
   یک کلمه‌ی معادل می‌رسد.
3. مرحله‌ی بعدی (هنوز شروع نشده): ابزار «لوح روحانی» — تولید لوح از طریق
   استخراج/تکسیر از متن قرآن.

## ساختار کتاب جفر جامع (قطعی، تأییدشده)

- ۲۸ جزء × ۲۸ صفحه × ۲۸ سطر × ۲۸ خانه؛ هر خانه ۴ حرف.
- الفبای ۲۸ حرفی به ترتیبِ «ابجد هوز حطی کلمن سعفص قرشت ثخذ ضظغ»:
  `ا ب ج د ه و ز ح ط ی ک ل م ن س ع ف ص ق ر ش ت ث خ ذ ض ظ غ`
- فرمول: `خانه(جزء=j، صفحه=s، سطر=t، خانه=k) = [حرف(j)، حرف(s)، حرف(t)، حرف(k)]`
  یعنی هر عدد (۱ تا ۲۸) مستقیماً به حرفِ هم‌ترازش در همان الفبا تبدیل می‌شود.
  مثال: جزء۱،صفحه۱،سطر۱،خانه۱ = «ا ا ا ا». جزء۲۸،صفحه۲،سطر۱۵،خانه۲۶ = «غ ب س ض».
- منبع اولیه‌ی این فرمول: https://hafeziyan.ir/?p=1942 (ربات‌ها بلاک هستند، از
  جست‌وجوی وب تأیید شد).
- کد: `getHouse(j,s,t,k)` و `getPage(j,s)` در `src/index.js`.

## جدولِ تهجی (املای حروف) — تأییدشده با عکس کاربر

منبع: عکسی که کاربر فرستاد (تهجی سنتیِ ۲۸ حرف). این جدول در `LETTER_SPELLING`
در `src/index.js` پیاده شده و باید همیشه با این نسخه یکی باشد:

| حرف | املا | حرف | املا | حرف | املا | حرف | املا |
|---|---|---|---|---|---|---|---|
| ا | الف | ه | ها | ک | کاف | ق | قاف |
| ب | با | و | واو | ل | لام | ر | را |
| ج | جیم | ز | زا | م | میم | ش | شین |
| د | دال | ح | حا | ن | نون | ت | تا |
| | | ط | طا | س | سین | ث | ثا |
| | | ی | یا | ع | عین | خ | خا |
| | | | | ف | فا | ذ | ذال |
| | | | | ص | صاد | ض | ضاد |
| | | | | | | ظ | ظا |
| | | | | | | غ | غین |

## دو روشِ «استخراج مستحصله»

### روش ۱: ملفوظی/ملبوبی/مسروری → آدرس خانه
- تفسیر ساختاریافته‌ی من از یک روش سنتیِ رایج (نه یک نسخه‌ی رسمی و واحد؛
  در تراث جفر از استادی به استادی فرق دارد). کد: تابع `estekhraj()`.
- ملفوظی = حروف خودِ متن. ملبوبی = بینه‌ی هر حرف (ادامه‌ی توضیح در روش ۲).
  مسروری = همان حروف ملفوظی به‌صورت معکوس. این‌ها امتزاج و جمع ابجد می‌شوند
  و با تقسیم‌های متوالی بر ۲۸ به آدرس (جزء/صفحه/سطر/خانه) می‌رسند.
- **این روش هنوز با کاربر به‌طور کامل confirm نشده** — فقط روش ۲ دقیق تأیید شده.

### روش ۲: ملفوظی/بینات → عدد → کلمه‌ی معادل (تأییدشده و دقیق)
این روش را کاربر قدم‌به‌قدم توضیح داد و با مثال «علی» تأیید کرد. کد: تابع‌های
`malfuziExpand()` و `binat()` در `src/index.js`.

1. **ملفوظی**: هر حرفِ کلمه را با املای کاملش (طبق جدول بالا) می‌نویسیم و پشت
   سر هم می‌چینیم.
   مثال: علی (ع، ل، ی) → عین + لام + یا → `ع ی ن ل ا م ی ا`
2. **بینات**: برای **تک‌تکِ حروفِ اصلیِ کلمه، جداگانه**، حرفِ اولِ املای خودش
   را حذف می‌کنیم (چون املا همیشه با خودِ حرف شروع می‌شود) و باقیمانده‌ها را
   پشت سر هم می‌چینیم. **نکته‌ی مهم: این کارِ حذف باید برای هر حرف جدا انجام
   شود، نه یک‌بار روی کلِ رشته‌ی ملفوظیِ ترکیبی** (این اشتباهِ اولیه‌ای بود که
   اصلاح شد).
   مثال: علی → عین−ع=«ین»، لام−ل=«ام»، یا−ی=«ا» → بینات = `ی ن ا م ا`
3. **مجموع**: مجموع ابجدِ کبیرِ بینات. برای «علی» = **۱۰۲** (تأییدشده توسط
   کاربر). این عدد را می‌توان یا با جمعِ مستقیمِ حروفِ بینات به‌دست آورد، یا
   ساده‌تر: `مجموع(ملفوظی) − مجموع(حروف اصلیِ کلمه)` — هر دو روش دقیقاً همین
   عدد را می‌دهند (در کد هر دو پیاده و کراس‌چک شده‌اند).
4. **کلمه‌ی معادل**: عددِ به‌دست‌آمده در یک فرهنگ لغت (بر اساسِ ابجدِ کبیرِ هر
   کلمه) جست‌وجو می‌شود؛ کلماتی با همین مجموع، «معادل» آن عدد هستند.

## فرهنگ لغتِ «کلمه‌ی معادل»

- فایل: `public/data/word-index.json` — نگاشتِ `{ "عدد": { "persian": [], "arabic": [], "names": [] } }`.
- ساخته می‌شود با `scripts/build-word-index.mjs` (باید **روی سیستمِ کاربر**
  اجرا شود، نه در sandbox، چون به دانلود از اینترنت نیاز دارد).
- منبع فارسی در `scripts/build-word-index.mjs` بسته‌ی PyPI
  `mnk-persian-words==1.5.0` است؛ فرهنگ عربی همچنان از فایل Hunspell مخزنِ
  `titoBouzout/Dictionaries` خوانده می‌شود.
- علاوه بر این، آرایه‌ی `NAMES` در همان اسکریپت شاملِ ~۱۸۰ نامِ رایجِ
  ایرانی/عربی (دستچین‌شده، نه از اینترنت) است.
- **کارِ باقی‌مانده**: افزودنِ کردی (سورانی) و ترکیِ آذریِ نوشته‌شده با خطِ
  فارسی. این زبان‌ها حروفِ اضافه دارند (مثلاً ڵ ڕ ۆ ێ در کردی، یا گ چ پ ژ که
  در فارسی هم هست) که باید در `NORMALIZE_MAP` به نزدیک‌ترین حرفِ ۲۸گانه نگاشت
  شوند. منبعِ دیکشنری برای این دو زبان هنوز پیدا/تأیید نشده.
- برای افزودنِ یک منبعِ جدید: فقط یک آیتم `{name, url}` به `DICTIONARY_SOURCES`
  در `scripts/build-word-index.mjs` اضافه کن.

## جست‌وجو با ۴ حرف (برای مرورِ کتاب)

- در تبِ «مرور کتاب»، کاربر می‌تواند ۴ حرف (مثلاً `غبسض`) تایپ کند تا اپ
  مستقیم به همان خانه برود (صفحه را باز کند + خانه را در جدول هایلایت کند).
- Endpoint: `GET /api/locate?text=...` — چهار حرف را نرمالایز می‌کند و
  اندیس‌شان در الفبا را مستقیماً به‌عنوان (جزء، صفحه، سطر، خانه) برمی‌گرداند
  (معکوسِ ساده‌ی فرمولِ `getHouse`).

## ساختار فایل‌ها

```
jafr-app/
├── .devcontainer/devcontainer.json   ← تنظیمات GitHub Codespaces
├── wrangler.toml
├── package.json
├── README.md
├── scripts/
│   ├── build-word-index.mjs          ← باید روی سیستم کاربر اجرا شود
│   └── dev-codespaces.sh
├── src/index.js                       ← همه‌ی منطق + API routes
└── public/
    ├── index.html / style.css / app.js
    └── data/word-index.json           ← خروجیِ اسکریپتِ بالا
```

## وضعیت فعلی / قدم بعدی

- همه‌ی موارد بالا پیاده و تست‌شده (با node --check و اسکریپت‌های تستِ محلی)،
  ولی هنوز روی Cloudflare واقعی دیپلوی نشده (کاربر گفت بعداً دیپلوی را تست
  می‌کند).
- مرحله‌ی بعدی که کاربر گفته: ساختِ ابزار «لوح روحانی» (تولید لوح از طریق
  استخراج/تکسیر از متنِ قرآن). هنوز هیچ کاری روی این بخش انجام نشده؛
  الگوریتمِ دقیقش هم هنوز مشخص نیست — باید از کاربر پرسیده شود، دقیقاً مثلِ
  روندی که برای «جفر جامع» طی شد (قدم‌به‌قدم، با مثال، با تأییدِ خودِ کاربر
  قبل از پیاده‌سازیِ نهایی).
- توابعِ خالص (`getHouse`, `computeAbjad`, `normalizeText`, `malfuziExpand`,
  `binat`) از `src/index.js` export شده‌اند تا در ابزارِ بعدی هم قابلِ
  استفاده‌ی مجدد باشند.
